import { SimplePage } from "../../components/simple-page";

export default function PricingPage() {
  return (
    <SimplePage
      title="Pricing"
      description="Simple plans that scale from solo builders to enterprise teams with transparent per-build economics."
    />
  );
}
